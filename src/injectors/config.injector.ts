/**
 * Automaically injects configuration into the property/parameter in whichever class (Datasource, Model, ...etc)
 * 
 * @param name Name of the configuration file
 */
export const config = (name: string) => {
	//
}

/**
 * Namespaces configuration injectors
 */
export namespace Config {
	/**
	 * Inject datasource configuration into a property/parameter
	 * 
	 * @param name Name of the configuration file
	 */
	export const Datasource = (name: string) => {
		//
	}
}